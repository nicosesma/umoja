import {expect, knex, commands, queries} from '../../setup'

describe('database.queries', () => {
  describe('createUser', () => {
    it('creates a user', () => {
      console.log('need to get test configured')
    })
  })
})
