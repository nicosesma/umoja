import {expect, knex, commands, queries} from '../../setup'

describe('database.queries', () => {
  describe('getAllVendorSpots()', () => {
    it('gets a list of all vendor spots', () => {
      console.log('need to run seed data')
    })
  })
})
